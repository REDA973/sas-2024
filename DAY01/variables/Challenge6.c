#include <stdio.h>

int main()
{
   float a,b;
   printf("entrer la valeur de de a:");
   scanf("%f",a);
   printf("entrer la valeur de b:");
   scanf("%f",b);
   a + b , a - b , a * b , a / b ;
   printf("a + b:%f \n a - b:%f\n a * b:%f \na / b:%f",a + b,a - b,a * b, a / b);

}